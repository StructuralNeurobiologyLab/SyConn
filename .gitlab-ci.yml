# TODO: refactor and remove tags
stages:
  - build
  - test
  - cleanup

test:
  stage: test
  script:
    - pytest --cache-clear
    - source ~/.bashrc
    - conda activate pysyintegration
    - pip install --upgrade --no-deps -v -e .
    - pytest
    - pytest --cache-clear
  except:
    - master
  tags:
    - default

test-full:
  stage: test
  script:
    - pytest --cache-clear
    - source ~/.bashrc
    - conda activate pysyintegration_m
    - pip install --upgrade --no-deps -v -e .
    - pytest
    - pytest tests/test_run.py  # full example run
    - pytest --cache-clear
  only:
    - master
  tags:
    - master

build:
  stage: build
  script:
    - conda env create -f environment.yml -n pysyintegration_m python --force
    - source ~/.bashrc
    - conda activate pysyintegration_m
  only:
    - master
  tags:
    - master

cleanup:
  stage: cleanup
  script:
    - conda remove --yes -n pysyintegration_m --all
  when: always
  only:
    - master
  tags:
    - master

pylint:
  stage: test
  before_script:
    - pip install pylint pylint-exit anybadge
  script:
    - mkdir ./pylint
    - pylint --ignore=ui --ignore=batchjob_scripts --ignore=cnn --output-format=text syconn | tee ./pylint/pylint.log || pylint-exit $?
    - PYLINT_SCORE=$(sed -n 's/^Your code has been rated at \([-0-9.]*\)\/.*/\1/p' ./pylint/pylint.log)
    - anybadge --label=Pylint --file=pylint/pylint.svg --value=$PYLINT_SCORE 2=red 4=orange 8=yellow 10=green
    - echo "Pylint score is $PYLINT_SCORE"
  artifacts:
    paths:
      - ./pylint/
  tags:
    - default
  allow_failure: true